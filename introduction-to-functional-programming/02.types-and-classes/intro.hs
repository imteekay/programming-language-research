-- A type is a name for a collection of related values
-- Types in Haskell
-- the value `e` with type `t` looks like `e :: t`

-- Basic Types: Bool, Char, String, Int, Integer, Bool, Float
-- name :: String

-- List Types: a sequence of values of the same type
-- [False, True, True, False] :: [Bool]
-- ['a', 'b', 'c', 'd'] :: [Char]

-- Tuple Types: sequence of value with different types
-- (True, 'a', False) :: (Bool, Char, Bool)
