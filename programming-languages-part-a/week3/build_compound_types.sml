(* Base type: int, bool, unit, char *)
(* Compound types: tuples, list, options *)

(* each-of types: int * bool contains an int and a bool *)
(* one-of types: int option contains an int or it contains no data *)
(* all three building blocks: int list contains an int and another int or it contains no data *)
